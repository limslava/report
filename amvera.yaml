meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run amvera:build

run:
  command: npm run amvera:start
  containerPort: 3000
  persistenceMount: /data
