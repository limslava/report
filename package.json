{
  "name": "report-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "amvera:build": "npm --prefix frontend ci --no-audit --no-fund && npm --prefix frontend run build && npm --prefix backend ci --no-audit --no-fund && npm --prefix backend run build",
    "amvera:start": "node backend/dist/index.js"
  }
}
